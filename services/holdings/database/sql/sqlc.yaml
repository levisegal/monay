version: "2"
sql:
  - engine: "sqlite"
    queries: "queries"
    schema: "schema.sql"
    gen:
      go:
        package: "db"
        out: "../../gen/db"
        emit_json_tags: true
        emit_empty_slices: true

