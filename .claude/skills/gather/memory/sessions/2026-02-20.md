## 17:18 — etrade: Profile lock on resume
Tried to resume from previous session — Chrome showed "Something went wrong when opening your profile" dialog. The previous session must have used `--profile` mode and it got locked/corrupted. Had to `kill-all` and reopen with `--headed` (no persistence). User will need to re-login.

## 17:19 — etrade: Login URL
`/home` lands on marketing page, not login. Direct login URL is `https://us.etrade.com/etx/pxy/login`.

## 17:20 — etrade: No intro modal on second session
Activity page did not show the "Introducing the new Activity experience" modal this time. Either it's per-session (first session already dismissed it) or it's stored in a cookie. Since we're using `--headed` (no persistence), it may be server-side per-login-session.

## 17:20 — etrade: Duration resets on account switch
Changing the Accounts dropdown resets Duration back to "Last 30 Days". Must re-select Current Year / Prior Year after each account switch.

## 17:21 — etrade: Download format resets every time
The "Select data format" dialog always defaults to "Spreadsheet format including Microsoft Excel". Must switch to "CSV file" for every single download — the selection is not sticky.

## 17:22 — etrade: joint-3652 CSV has extra disclaimer
The joint-3652 (GILD-linked) CSV includes a long disclaimer paragraph about Morgan Stanley & Co. and Externally Held accounts with activity date vs processing date. Other accounts don't have this. Likely because it's linked to a stock plan.

## 17:28 — etrade: All downloads completed successfully
8 CSVs downloaded across 4 accounts (2025 + 2026 each). Browser survived all downloads without dying — no crashes this session. The `--headed` (no profile) approach was stable throughout.

## 17:29 — etrade: Ref-based clicking pattern
playwright-cli's named selectors (e.g., `combobox "Duration"`) often fail with "not found in current page snapshot" even when the element exists. Using element refs (e.g., `e321`) from the snapshot YAML is much more reliable. Always read the snapshot first and use refs.
